const state = {
    user: {}
}
const mutations = {
    setUser(state,data) {
        state.user = data
    }
}
const actions = {
    
}
const getters = {
    
}
export default({
    state,
    getters,
    mutations,
    actions
})